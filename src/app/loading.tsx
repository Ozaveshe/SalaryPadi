export default function Loading() {
  return (
    <div className="site-shell" aria-live="polite" aria-busy="true">
      <div className="notice">Loading the latest verified information…</div>
    </div>
  );
}
